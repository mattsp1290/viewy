# Root nim.cfg, committed on purpose: editor language servers type-check each
# file as its own root and need the same `src` search path nimble gives the
# build, or every `import viewy/...` shows as broken in the editor.
--path:"src"
